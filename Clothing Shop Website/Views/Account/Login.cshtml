@{
    ViewData["Title"] = "Đăng nhập";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Styles {
    <link rel="stylesheet" href="~/css/dang-nhap.css" asp-append-version="true" />
}

<div class="auth-layout">
    <!-- LEFT: Visual panel -->
    <div class="auth-visual">
        <img src="https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=900&q=85" alt="NEVA">
        <div class="visual-overlay"></div>
        <div class="visual-content">
            <div class="visual-top">
                <div class="crest-area">
                    <div class="crest">
                        <div class="crest-outer-ring"></div>
                        <div class="crest-inner-ring"></div>
                        <div class="crest-center"><span>N</span></div>
                    </div>
                </div>
                <div class="visual-brand">NEVA</div>
                <div class="visual-tagline">Haute Couture · Vietnamese Fashion</div>
            </div>
            <div class="visual-quote">
                "Mỗi bộ trang phục là một tác phẩm nghệ thuật,<br>
                được tạo ra để tôn vinh vẻ đẹp của người mặc."
            </div>
        </div>
    </div>

    <!-- RIGHT: Form panel -->
    <div class="auth-form-wrap">
        <div class="auth-form">
            <!-- Tabs -->
            <div class="auth-tabs">
                <button class="auth-tab active" data-tab="login" onclick="switchTab('login')">Đăng nhập</button>
                <button class="auth-tab" data-tab="register" onclick="switchTab('register')">Tạo tài khoản</button>
            </div>

            <!-- LOGIN TAB -->
            <div class="tab-pane active" id="loginTab">
                <div class="f-group">
                    <label class="f-label">Số điện thoại</label>
                    <input class="f-input" type="tel" id="loginPhone" placeholder="09xx xxx xxx" autocomplete="tel">
                    <span class="f-err" id="loginPhoneErr"></span>
                </div>
                <div class="f-group">
                    <label class="f-label">Mật khẩu</label>
                    <div class="f-input-wrap">
                        <input class="f-input" type="password" id="loginPass" placeholder="••••••••" autocomplete="current-password">
                        <button class="eye-btn" type="button" onclick="togglePass('loginPass')">
                            <svg viewBox="0 0 24 24"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" /><circle cx="12" cy="12" r="3" /></svg>
                        </button>
                    </div>
                    <span class="f-err" id="loginPassErr"></span>
                    <a href="#" class="forgot-link">Quên mật khẩu?</a>
                </div>
                <button class="btn-submit" onclick="doLogin()">Đăng nhập</button>
                <div class="f-divider"><span>hoặc</span></div>
                <p style="text-align:center;font-size:12px;color:rgba(250,248,242,.28);letter-spacing:1px">
                    Chưa có tài khoản?
                    <a href="#" onclick="switchTab('register')" style="color:var(--gold);border-bottom:1px solid rgba(201,168,76,.3);padding-bottom:1px">
                        Tạo tài khoản ngay
                    </a>
                </p>
            </div>

            <!-- REGISTER TAB -->
            <div class="tab-pane" id="registerTab">
                <div class="f-group">
                    <label class="f-label">Họ và tên</label>
                    <input class="f-input" type="text" id="regName" placeholder="Nguyễn Thị A" autocomplete="name">
                    <span class="f-err" id="regNameErr"></span>
                </div>
                <div class="f-group">
                    <label class="f-label">Số điện thoại</label>
                    <input class="f-input" type="tel" id="regPhone" placeholder="09xx xxx xxx" autocomplete="tel">
                    <span class="f-err" id="regPhoneErr"></span>
                </div>
                <div class="f-group">
                    <label class="f-label">Mật khẩu</label>
                    <div class="f-input-wrap">
                        <input class="f-input" type="password" id="regPass" placeholder="Tối thiểu 6 ký tự" autocomplete="new-password">
                        <button class="eye-btn" type="button" onclick="togglePass('regPass')">
                            <svg viewBox="0 0 24 24"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" /><circle cx="12" cy="12" r="3" /></svg>
                        </button>
                    </div>
                    <span class="f-err" id="regPassErr"></span>
                </div>
                <div class="f-group">
                    <label class="f-label">Xác nhận mật khẩu</label>
                    <div class="f-input-wrap">
                        <input class="f-input" type="password" id="regConfirm" placeholder="Nhập lại mật khẩu" autocomplete="new-password">
                        <button class="eye-btn" type="button" onclick="togglePass('regConfirm')">
                            <svg viewBox="0 0 24 24"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" /><circle cx="12" cy="12" r="3" /></svg>
                        </button>
                    </div>
                    <span class="f-err" id="regConfirmErr"></span>
                </div>
                <button class="btn-submit" style="margin-top:22px" onclick="doRegister()">Tạo tài khoản</button>
                <p class="terms-text">
                    Bằng cách đăng ký, bạn đồng ý với
                    <a href="#">Điều khoản</a> và
                    <a href="#">Chính sách bảo mật</a> của NEVA.
                </p>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/dang-nhap.js" asp-append-version="true"></script>
}
